import 'package:flutter/material.dart';

class StressTracking extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text(
        'Stress Tracking Page',
        style: TextStyle(fontSize: 24),
      ),
    );
  }
}
